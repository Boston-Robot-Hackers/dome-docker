# Project defaults. Override DOME_USER/DOME_TARGET in manifest/user.txt (gitignored).
ROS_DISTRO=kilted
UBUNTU_CODENAME=noble
DOME_USER=robot
# DOME_TARGET=pi|vm — pi installs Pi-hardware packages/repos/overlays; vm skips them.
DOME_TARGET=pi
# SWAP_SIZE_MB — Pi-only swapfile size in MB, guards against colcon build OOM. 0 disables.
SWAP_SIZE_MB=2048
# DOME_MODE=native|docker — native skips Docker install/dome.service (Scenarios 1/2).
# Only Scenario 3 (docker-howto.md) sets DOME_MODE=docker in manifest/user.txt.
DOME_MODE=native
